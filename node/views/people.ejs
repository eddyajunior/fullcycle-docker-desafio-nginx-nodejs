<%
console.log(data)
%>
<html>
    <body>
        <h1>Full Cycle Rocks!</h1>
        <% if(data.length) {%>
        <table>
            <tr>
                <td>Id</td>
                <td>Nome</td>
            </tr>
            <% for (var i=0; i < data.length; i++){ %>
            <tr>
                <td><%=data[i].id%></td>
                <td><%=data[i].name%></td>
            </tr>
        <% }%>
        </table>
        <% } %>
    </body>
</html>